{
  // 배포용 tsconfig는 테스트를 제외하지만, 타입 검사는 테스트까지 봐야 한다.
  // `npm run type`이 이 설정을 쓴다.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src"],
  "exclude": []
}
